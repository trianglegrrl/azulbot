<%= form_with(model: contact, local: true) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
        <% contact.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :phone %>
    <%= form.text_field :phone %>
  </div>

  <div class="field">
    <%= form.label :relationship_state %>
    <%= form.text_field :relationship_state %>
  </div>

  <div class="field">
    <%= form.label :conversation_state %>
    <%= form.text_field :conversation_state %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
